<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 科技感主题颜色定义 -->
    <Color x:Key="CyberDarkBackground">#0D1117</Color>
    <Color x:Key="CyberCardBackground">#161B22</Color>
    <Color x:Key="CyberSurfaceBackground">#21262D</Color>
    <Color x:Key="CyberBorderColor">#30363D</Color>
    <Color x:Key="CyberAccentBlue">#58A6FF</Color>
    <Color x:Key="CyberAccentCyan">#39D0D8</Color>
    <Color x:Key="CyberAccentPurple">#A855F7</Color>
    <Color x:Key="CyberTextPrimary">#F0F6FC</Color>
    <Color x:Key="CyberTextSecondary">#8B949E</Color>
    <Color x:Key="CyberTextMuted">#656D76</Color>
    <Color x:Key="CyberGreen">#3FB950</Color>
    <Color x:Key="CyberRed">#F85149</Color>
    <Color x:Key="CyberYellow">#D29922</Color>
    <Color x:Key="CyberOrange">#FF7B72</Color>

    <!-- 渐变定义 -->
    <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#A855F7" Offset="0"/>
        <GradientStop Color="#3B82F6" Offset="0.5"/>
        <GradientStop Color="#06B6D4" Offset="1"/>
    </LinearGradientBrush>
    
    <LinearGradientBrush x:Key="CyberHeaderGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#A855F7" Offset="0"/>
        <GradientStop Color="#3B82F6" Offset="0.5"/>
        <GradientStop Color="#06B6D4" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="CyberButtonGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#58A6FF" Offset="0"/>
        <GradientStop Color="#39D0D8" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="CyberAccentGradient" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#A855F7" Offset="0"/>
        <GradientStop Color="#3B82F6" Offset="0.5"/>
        <GradientStop Color="#06B6D4" Offset="1"/>
    </LinearGradientBrush>

    <!-- 画刷定义 -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource CyberDarkBackground}"/>
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource CyberCardBackground}"/>
    <SolidColorBrush x:Key="SurfaceBackgroundBrush" Color="{StaticResource CyberSurfaceBackground}"/>
    <SolidColorBrush x:Key="ForegroundBrush" Color="{StaticResource CyberTextPrimary}"/>
    <SolidColorBrush x:Key="SecondaryForegroundBrush" Color="{StaticResource CyberTextSecondary}"/>
    <SolidColorBrush x:Key="MutedForegroundBrush" Color="{StaticResource CyberTextMuted}"/>
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource CyberBorderColor}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource CyberAccentBlue}"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource CyberGreen}"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource CyberRed}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource CyberYellow}"/>
    <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource CyberAccentCyan}"/>

    <!-- JSON语法高亮 - 霓虹灯风格 -->
    <SolidColorBrush x:Key="JsonObjectBrush" Color="#A855F7"/>
    <SolidColorBrush x:Key="JsonArrayBrush" Color="#3FB950"/>
    <SolidColorBrush x:Key="JsonStringBrush" Color="#FF7B72"/>
    <SolidColorBrush x:Key="JsonNumberBrush" Color="#D29922"/>
    <SolidColorBrush x:Key="JsonBooleanBrush" Color="#39D0D8"/>
    <SolidColorBrush x:Key="JsonNullBrush" Color="#8B949E"/>
    <SolidColorBrush x:Key="JsonKeyBrush" Color="#58A6FF"/>

    <!-- 阴影效果 -->
    <DropShadowEffect x:Key="CyberCardShadow" 
                      Color="#000000" 
                      Direction="270" 
                      ShadowDepth="4" 
                      BlurRadius="12" 
                      Opacity="0.3"/>

    <DropShadowEffect x:Key="CyberGlowEffect" 
                      Color="#58A6FF" 
                      Direction="0" 
                      ShadowDepth="0" 
                      BlurRadius="8" 
                      Opacity="0.6"/>

    <!-- 现代化按钮样式 -->
    <Style x:Key="CyberButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource CyberButtonGradient}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="Margin" Value="4,2"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Effect" Value="{StaticResource CyberCardShadow}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="MainBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <Border.RenderTransform>
                            <ScaleTransform x:Name="ScaleTransform" ScaleX="1" ScaleY="1"/>
                        </Border.RenderTransform>
                        <ContentPresenter HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"
                                        TextBlock.FontWeight="{TemplateBinding FontWeight}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                                       Storyboard.TargetProperty="ScaleX"
                                                       To="1.05" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                                       Storyboard.TargetProperty="ScaleY"
                                                       To="1.05" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                                       Storyboard.TargetProperty="ScaleX"
                                                       To="1" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="ScaleTransform"
                                                       Storyboard.TargetProperty="ScaleY"
                                                       To="1" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ScaleTransform" Property="ScaleX" Value="0.95"/>
                            <Setter TargetName="ScaleTransform" Property="ScaleY" Value="0.95"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 科技感卡片样式 -->
    <Style x:Key="CyberCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Effect" Value="{StaticResource CyberCardShadow}"/>
        <Setter Property="Padding" Value="16"/>
    </Style>

    <!-- 发光文本样式 -->
    <Style x:Key="CyberGlowTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="Effect" Value="{StaticResource CyberGlowEffect}"/>
        <Setter Property="FontWeight" Value="Medium"/>
    </Style>

    <!-- 现代化TreeView样式 -->
    <Style x:Key="CyberTreeViewStyle" TargetType="TreeView">
        <Setter Property="Background" Value="{StaticResource SurfaceBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
        <Setter Property="ScrollViewer.IsDeferredScrollingEnabled" Value="True"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TreeView">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                        <ScrollViewer Focusable="False"
                                    CanContentScroll="True"
                                    Padding="8">
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 科技感TreeViewItem样式 -->
    <Style x:Key="CyberTreeViewItemStyle" TargetType="TreeViewItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="Margin" Value="0,1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TreeViewItem">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        
                        <Border Name="ItemBorder" 
                                Grid.Row="0"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- 现代化展开按钮 -->
                                <ToggleButton Name="Expander"
                                            Grid.Column="0"
                                            ClickMode="Press"
                                            IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                            Width="20" Height="20"
                                            Margin="0,0,8,0"
                                            Background="Transparent"
                                            BorderThickness="0">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="Focusable" Value="False"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border Background="Transparent" 
                                                                CornerRadius="4"
                                                                Width="20" Height="20">
                                                            <Path Name="ExpandPath"
                                                                  HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"
                                                                  Data="M 6 4 L 10 8 L 6 12 Z"
                                                                  Fill="{StaticResource AccentBrush}"
                                                                  Stretch="Uniform"
                                                                  Width="8" Height="8">
                                                                <Path.RenderTransform>
                                                                    <RotateTransform x:Name="ExpandRotation" Angle="0"/>
                                                                </Path.RenderTransform>
                                                            </Path>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Trigger.EnterActions>
                                                                    <BeginStoryboard>
                                                                        <Storyboard>
                                                                            <DoubleAnimation Storyboard.TargetName="ExpandRotation"
                                                                                           Storyboard.TargetProperty="Angle"
                                                                                           To="90" Duration="0:0:0.2"/>
                                                                        </Storyboard>
                                                                    </BeginStoryboard>
                                                                </Trigger.EnterActions>
                                                                <Trigger.ExitActions>
                                                                    <BeginStoryboard>
                                                                        <Storyboard>
                                                                            <DoubleAnimation Storyboard.TargetName="ExpandRotation"
                                                                                           Storyboard.TargetProperty="Angle"
                                                                                           To="0" Duration="0:0:0.2"/>
                                                                        </Storyboard>
                                                                    </BeginStoryboard>
                                                                </Trigger.ExitActions>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                                
                                <!-- 内容 -->
                                <ContentPresenter Name="PART_Header"
                                                Grid.Column="1"
                                                ContentSource="Header"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        
                        <!-- 子项容器 -->
                        <ItemsPresenter Name="ItemsHost"
                                      Grid.Row="1"
                                      Margin="28,0,0,0"/>
                    </Grid>
                    
                    <ControlTemplate.Triggers>
                        <!-- 鼠标悬停效果 -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="#21262D" Opacity="0.8"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        
                        <!-- 选中效果 -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#58A6FF" Offset="0" OpacityMask="#20"/>
                                        <GradientStop Color="#39D0D8" Offset="1" OpacityMask="#20"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            <Setter TargetName="ItemBorder" Property="BorderThickness" Value="1"/>
                        </Trigger>
                        
                        <!-- 没有子项时隐藏展开按钮 -->
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                        
                        <!-- 折叠状态隐藏子项 -->
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 科技感进度条样式 -->
    <Style x:Key="CyberProgressBarStyle" TargetType="ProgressBar">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Border Background="{StaticResource SurfaceBackgroundBrush}" 
                            CornerRadius="12" 
                            BorderThickness="1" 
                            BorderBrush="{StaticResource BorderBrush}">
                        <Grid>
                            <Border Name="PART_Track" 
                                    Background="Transparent" 
                                    CornerRadius="12"/>
                            <Border Name="PART_Indicator" 
                                    Background="{StaticResource CyberButtonGradient}" 
                                    CornerRadius="12" 
                                    HorizontalAlignment="Left">
                                <Border.Effect>
                                    <DropShadowEffect Color="#58A6FF" 
                                                    Direction="0" 
                                                    ShadowDepth="0" 
                                                    BlurRadius="8" 
                                                    Opacity="0.8"/>
                                </Border.Effect>
                            </Border>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>